# Remaining Python utilities (feeds, correlation helpers, etc.). Orchestration and HTTP are Rust-only:
#   weissman-server  — production API + Command Center
#   weissman-worker  — background work
#   fingerprint_engine — CLI engines (no `serve` subcommand)
#
# Install: pip install -r requirements.txt
# To upgrade: edit requirements.in and run: pip-compile --generate-hashes requirements.in

# --- Database & ORM (legacy scripts / tooling) ---
SQLAlchemy==2.0.49
aiosqlite==0.22.1
asyncpg==0.31.0
psycopg2-binary==2.9.12
alembic==1.18.4

# --- HTTP & networking ---
requests[socks]==2.31.0
aiohttp==3.13.5
tenacity==9.1.4

# --- Auth & security (bcrypt<5 for passlib) ---
pyotp==2.9.0
bcrypt==3.2.2
passlib[bcrypt]==1.7.4
itsdangerous==2.2.0
Authlib==1.7.0

# --- Config & env ---
python-dotenv==1.2.2
pydantic==2.13.3
pydantic-settings==2.14.0
PyYAML==6.0.1

# --- Reporting & PDF ---
weasyprint==68.1
Markdown==3.10.2
openpyxl==3.1.5

# --- Scheduling (in-process; not Celery) ---
APScheduler==3.11.2

# --- CLI & logging ---
rich==13.7.1

# --- Scraping & parsing ---
beautifulsoup4==4.14.3
