# Remaining Python utilities (feeds, correlation helpers, etc.). Orchestration and HTTP are Rust-only:
#   weissman-server  — production API + Command Center
#   weissman-worker  — background work
#   fingerprint_engine — CLI engines (no `serve` subcommand)
#
# Install: pip install -r requirements.txt

# --- Database & ORM (legacy scripts / tooling) ---
sqlalchemy
aiosqlite
asyncpg
psycopg2-binary
alembic

# --- HTTP & networking ---
requests
requests[socks]
aiohttp
tenacity
redis>=5.0.0

# --- Auth & security (bcrypt<5 for passlib) ---
pyotp
bcrypt<5.0.0
passlib[bcrypt]
itsdangerous
authlib

# --- Config & env ---
python-dotenv
pydantic
pydantic-settings
pyyaml

# --- Reporting & PDF ---
weasyprint
markdown
openpyxl

# --- Scheduling (in-process; not Celery) ---
apscheduler

# --- CLI & logging ---
rich

# --- Scraping & parsing ---
beautifulsoup4

# --- Testing & QA ---
pytest>=9.1.1
pytest-cov>=7.1.0
pytest-asyncio>=0.21.0
pytest-mock>=3.15.1

# --- Monitoring & Metrics ---
prometheus-client>=0.25.0

# --- Secrets Management ---
hvac>=2.4.0  # HashiCorp Vault client
cryptography>=49.0.0  # Field-level encryption

# NOTE: Flask and flask-swagger-ui removed - Swagger UI now served by Rust backend
# (see openapi_generator.py for optional standalone Flask utility if needed for development)

# --- E2E Testing ---
playwright>=1.60.0

# --- SIEM Integration ---
# NOTE: no module imports `elasticsearch` anywhere in the tree (production is Rust-only; the
# Python layer under legacy/ is deprecated). Kept here — with the floor raised past the 8.x
# line — rather than removed, since dropping a declared integration is a product decision.
elasticsearch>=9.4.1
