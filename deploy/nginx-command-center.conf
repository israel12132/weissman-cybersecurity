server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    location /command-center/ {
        alias /usr/share/nginx/html/command-center/;
        try_files $uri $uri/ /command-center/index.html;
    }
    location = / {
        return 302 /command-center/;
    }
}
