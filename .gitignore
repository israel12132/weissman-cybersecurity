.env
.envrc.private
.direnv/
result
result-*
.venv/
__pycache__/
*.pyc
data/
reports/
*.db
logs/
.e2e-stack/
evidence-pack/
frontend/playwright-report/
frontend/test-results/
frontend/blob-report/
frontend/dist/
target/
node_modules/
result/
.env
target/
node_modules/
result/
.env
.venv/

# Runtime scheduler artifacts
celerybeat-schedule
celerybeat-schedule-shm
celerybeat-schedule-wal
# Downloaded binaries/artifacts
*.deb

# Sensitive config and live environment files
config.yaml
.envrc
.env.dev
.env.staging
.env.production
monitoring/prometheus-web.generated.yml

# Runtime data files
existing_ids.txt

# Compiled binaries in repo root (anchored to root so they do NOT ignore the
# `crates/weissman-*` / `backend/weissman-*` source directories).
/weissman
/weissman-*

# Compiled endpoint-agent binaries (build artifacts ~5MB each; the source lives
# in crates/weissman-agent/ and IS tracked).
bin/agents/*/weissman-agent

# Build output
fingerprint_engine/target/

# Runtime data directories (already listed above, ensure explicit)
data/
reports/

# ...but scripts/data holds version-controlled vendored data (the MITRE ATT&CK index
# + migration ledger the coverage gate reads), not runtime output — keep it tracked.
!scripts/data/
!scripts/data/**

# Company legal details (copy from deploy/company.details.example.json)
deploy/company.details.json

# Redis snapshot artifacts (written by a local/e2e redis-server into the CWD)
dump.rdb
*.rdb

# Playwright storage-state (regenerated each run; never commit session tokens)
**/tests-e2e/.auth/

# Stale member lock — the workspace build uses the root Cargo.lock (see deploy/*.Dockerfile).
fingerprint_engine/Cargo.lock
