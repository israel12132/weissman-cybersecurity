# Optional overrides merged into repo-root `.env` by the operator (copy keys by hand).
# `./start_weissman.sh` generates role-separated DSNs and secrets in `.env` itself.
#
# Ask Weissman + Supreme Council — point at a real OpenAI-compatible endpoint:
# WEISSMAN_LLM_BASE_URL=http://host.docker.internal:11434/v1
# WEISSMAN_LLM_API_KEY=
# WEISSMAN_LLM_MODEL=llama3.2
# WEISSMAN_NL_QUERY_MODEL=llama3.2
