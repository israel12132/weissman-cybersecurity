<!-- GENERATED by scripts/sync_doc_metrics.mjs — do not edit by hand. Run: node scripts/sync_doc_metrics.mjs -->
# Platform metrics (code-derived)

These numbers are generated from source by `scripts/sync_doc_metrics.mjs`.
Do not hand-edit. CI runs `--check` to fail the build on drift. Other docs
should link here rather than copying figures.

| Metric | Value | Source of truth |
|--------|-------|-----------------|
| Production engine IDs | 583 | `scripts/engine_reality_audit.mjs` |
| — real live probes | 321 | same (313 distinct impls) |
| — aliases | 204 | same |
| — agent-required | 58 | same |
| Rust test fns (`#[test]`/`#[tokio::test]`, incl. parameterized) | 3109 | grep over workspace |
| HTTP route registrations (`.route(`) | 363 | grep over `fingerprint_engine/src` + `backend` |
| Migrations — `crates/weissman-db/migrations` | 155 | `ls crates/weissman-db/migrations` |
| Migrations — `fingerprint_engine/migrations` | 155 | `ls fingerprint_engine/migrations` |

**Reading the engine count honestly:** the 583 catalog IDs resolve to
**313 distinct probe implementations** — 204 are
aliases that re-tag a canonical probe, 58 are agent-required host
techniques, and several "real" IDs delegate to a shared runner (e.g. APT-group
surface profiles). The catalog number reflects breadth of technique coverage, not
583 independent scanners. Every ID still maps to a real execution path
(`engine_reality_audit.mjs` fails on any `no_path`).

> Verify locally: `node scripts/sync_doc_metrics.mjs --check`.
