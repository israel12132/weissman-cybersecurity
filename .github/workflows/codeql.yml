name: CodeQL

# Static application security testing (SAST) for the JS/TS command center and the
# Python tooling tree. Findings surface in the repo Security tab. Rust is covered by
# clippy's correctness/suspicious gate + cargo-audit in ci.yml.

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  schedule:
    # Weekly full scan so newly published CodeQL queries run against untouched code.
    - cron: "27 4 * * 1"

permissions:
  contents: read

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      actions: read
      contents: read
    strategy:
      fail-fast: false
      matrix:
        language: ["javascript-typescript", "python"]
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4.36.3
        with:
          languages: ${{ matrix.language }}
          queries: security-and-quality

      - name: Autobuild
        uses: github/codeql-action/autobuild@v4.36.3

      - name: Perform CodeQL analysis
        uses: github/codeql-action/analyze@v4.36.3
        with:
          category: "/language:${{ matrix.language }}"
